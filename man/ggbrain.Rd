% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-brain.R, R/geom-brain-polygon.R,
%   R/stat-brain.R
\docType{data}
\name{geom_brain}
\alias{geom_brain}
\alias{GeomBrain}
\alias{StatBrain}
\title{Plot brain atlas regions}
\usage{
geom_brain(
  mapping = aes(),
  data = NULL,
  atlas,
  hemi = NULL,
  view = NULL,
  position = position_brain(),
  context = TRUE,
  fun = mean,
  show.legend = NA,
  inherit.aes = TRUE,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{A data.frame containing variables to map. If \code{NULL}, the atlas
is plotted without user data. Add a facet with \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} to
draw one brain per group.}

\item{atlas}{A \code{ggseg_atlas} object (e.g. \code{dk()}, \code{aseg()}, \code{tracula()}).}

\item{hemi}{Character vector of hemispheres to include (e.g. \code{"left"},
\code{"right"}). Defaults to all hemispheres in the atlas.}

\item{view}{Character vector of views to include, as recorded in the atlas
data. For cortical atlases: \code{"lateral"}, \code{"medial"}. For subcortical/tract
atlases: slice identifiers like \code{"axial_3"}. Defaults to all views.}

\item{position}{Position adjustment, either as a string or the result of
a call to \code{\link[=position_brain]{position_brain()}}.}

\item{context}{Keep the rest of the brain as a soft grey backdrop (\code{TRUE},
the default), or show only the regions you're plotting (\code{FALSE}).}

\item{fun}{Function used to combine multiple \code{data} rows that map to the same
atlas region, applied within each facet panel. Defaults to \code{\link[=mean]{mean()}}. Any
function reducing a vector to a single value works (e.g. \code{\link[=median]{median()}},
\code{\link[=max]{max()}}).}

\item{show.legend}{Logical. Should this layer be included in the legends?}

\item{inherit.aes}{Logical. If \code{FALSE}, overrides the default aesthetics
rather than combining with them.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_polygon]{ggplot2::geom_polygon()}}.}
}
\value{
A list of ggplot2 layer and coord objects.
}
\description{
Colour brain regions by your own values. Give \code{geom_brain()} an atlas like
\code{dk()} and a data frame, and it matches your values to the right regions and
lays out the brain views for you. No data? It just draws the atlas.
}
\details{
Regions are drawn in the order they appear in your \code{data}, so when outlines
overlap (e.g. mapping \code{colour} to a threshold with a wide \code{linewidth}) the
later rows draw on top. Reorder your data with \code{\link[dplyr:arrange]{dplyr::arrange()}} to control
the layering; regions you supply no value for stay underneath in atlas order.

Faceting works without any \code{\link[dplyr:group_by]{dplyr::group_by()}}: the atlas geometry is drawn
by \link{StatBrain}, which ggplot2 recomputes per panel, so the complete brain
appears in every facet. If your \code{data} has several rows for the same region
(e.g. one per subject), they are combined with \code{fun} (mean by default) before
drawing.
}
\section{GeomBrain ggproto}{

\code{GeomBrain} is the \link[ggplot2:Geom]{ggplot2::Geom} ggproto that renders brain atlas polygons.
It subclasses \link[ggplot2:Geom]{ggplot2::GeomPolygon} and supplies the brain defaults through
\code{default_aes} -- outline \code{colour} (grey35), \code{linewidth} (0.2), and \code{fill}
(grey) -- which apply when the user has not mapped those aesthetics but yield
to a mapping when present (ggsegverse/ggseg#160). The grey default fill is
why an atlas plotted without data renders grey, not palette-coloured. Used
internally by \code{\link[=geom_brain]{geom_brain()}}; not typically called directly.
}

\section{StatBrain ggproto}{

\code{StatBrain} is the \link[ggplot2:Stat]{ggplot2::Stat} that powers \code{\link[=geom_brain]{geom_brain()}} and
\code{\link[=stat_brain]{stat_brain()}}. Its \code{compute_panel()} receives one facet panel's user data,
aggregates the mapped values per atlas region with \code{fun}, and joins them onto
the full atlas geometry -- so every region renders in every panel whether or
not you supplied a value for it. You rarely reference it directly; use it via
\code{geom_brain(..., stat = StatBrain)} or \code{\link[=stat_brain]{stat_brain()}} when building a custom
layer.
}

\examples{
library(ggplot2)

ggplot() +
  geom_brain(atlas = dk())
}
\seealso{
\code{\link[=stat_brain]{stat_brain()}} for the stat-first spelling.
}
\keyword{datasets}
